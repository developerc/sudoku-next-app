import React, { FunctionComponent } from 'react';
import styles from '../styles/Sudoku.module.css';

export const VerticalPart: FunctionComponent = () => {

    return (
        <div className = {styles.verticalpart}></div>
    );
}